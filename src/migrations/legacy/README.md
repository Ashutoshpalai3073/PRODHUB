# Legacy Incutrack migrations

Superseded by `../sanyog/`. Kept for reference only — do NOT run these.
They assume a database that already has `startups` and `documents` (no migration
here creates either), so they cannot bootstrap a fresh project.
